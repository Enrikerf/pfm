@startuml

start
if (validId) then (no)
  :throw InvalidIdError/
  stop
elseif (batch Exists) then (no)
  :throw BatchNotFoundError/
  stop
elseif (has results) then (yes)
  :delete results/
else (no)
endif
    :delete batch/
end
@enduml